<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
	x:Class="Decrypt.MainWindow"
	x:Name="Window"
	Title="MainWindow"
	Width="640" Height="480">

    <Grid x:Name="LayoutRoot">
        <TextBox x:Name="_res_t" Margin="8,73,10,211" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto"/>
        <Button x:Name="button1" Content="Открыть..." HorizontalAlignment="Left" Height="29" Margin="8,8,0,0" VerticalAlignment="Top" Width="105" Click="Button_Click" Foreground="#FF838383"/>
        <TextBox x:Name="_res_t_rus" Margin="8,271,10,10" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto"/>
        <Label Content="RAW-output" HorizontalAlignment="Left" Height="31" Margin="10,42,0,0" VerticalAlignment="Top" Width="134"/>
        <Label Content="Something like a text" HorizontalAlignment="Left" Height="31" Margin="10,240,0,0" VerticalAlignment="Top" Width="134"/>
    </Grid>
</Window>